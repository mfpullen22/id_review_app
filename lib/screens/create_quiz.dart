import "package:flutter/material.dart";

class CreateQuizScreen extends StatelessWidget {
  const CreateQuizScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text("create quiz screen"),
      ],
    );
  }
}
